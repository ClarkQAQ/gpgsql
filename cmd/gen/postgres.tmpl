//go:build {{target}} && {{arch}}

// This file generated by cmd/gen/main.go - DO NOT EDIT

package release

import (
	_ "embed"
)

const (
	Target  = "{{target}}"
	Arch    = "{{arch}}"
	Version = "{{version}}"
    Sha256  = "{{sha256}}"

	InitdbBinary = "{{initdb}}"
	PgCliBinary  = "{{pg_ctl}}"
	PostgresBinary = "{{postgres}}"
)

var (
	//go:embed postgres-{{target}}-{{arch}}-{{version}}.tar.xz
	Archive []byte
)


